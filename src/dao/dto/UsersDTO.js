export default class UserDTO {
    constructor(id, name, email, bio, avatar) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.bio = bio;
      this.avatar = avatar;
    }
  }